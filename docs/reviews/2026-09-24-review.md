# Heekum Kim — feedback, sweep of 2026-09-24

One stage is in this one, Stage 1.2, and it moved because the file that was missing now exists. `model.xlsx` is in your repository, built the way your specification says — five sheets, 770 formulas, 59 named ranges — and at ten carrot, twenty mesclun and thirty tomato beds its formulas produce $42,761.66, the published optimum to the cent. The specification around it kept improving too: the rounded rates are derived now, labor is tiered, and the pasted prompt is gone. What holds it back is one unrun step. Solver has never been executed on the workbook, so the bed cells still read zero, profit reads minus $20,000, and the two-start table and the shadow prices sit blank waiting on it. Run Solver from both starting points, save in Excel, commit, and then write the audit of the build that came back. Stage 1.3 reads its numbers from this file, so those ten minutes unblock the rest of the case.

## Stage 1.2 — Spec, Build, Audit

Reviewed below, criterion by criterion. This is entered.

**CRITERION BY CRITERION**

* **Spec completeness — inputs, structure, calculation flow** — The rounded rates are out of your contract, derived and explained, and the pasted AI prompt is gone. Open: `CAR_HRS` is the printed 0.833 rather than 2.5/3.
* **Spec validation rules** — Labor is tiered, `TEMP_WORKERS` is a headcount check, and the 6,480-hour constraint is stated. Open: no tolerance on any acceptance figure, and nothing checks `EFFECTIVE_RATE` against `TOTAL_LABOR_COST`.
* **Workbook satisfies the contract** — Built the way your spec says — five sheets, 770 formulas, 59 named ranges — and at 10 / 20 / 30 the formulas give $42,761.66. What costs ten: Solver was never run, so the bed cells read 0 / 0 / 0, `TOTAL_PROFIT` reads −20,000, and the two-start table and shadow prices are blank.
* **Audit note** — No written audit of the build yet. The 4 is for `Checks` §1–2, which you ran against the built file.

**Repo:** https://github.com/heekum-Kim/Heekum-Kim
**Spec:** `capabilities/marginal-analysis/spec.md` (**13,040 B**, 177 lines — was 9,116 B on 09-18, and **2 B**, a bare newline, at the 2026-09-17 sweep) · **Workbook:** `capabilities/marginal-analysis/model.xlsx` (**29,568 B**, `cfdfadb` 2026-09-23 22:07 HST; 5 sheets, 770 formulas, 0 error cells, 59 named ranges) — *until 2026-09-24: none in any commit*

**WHAT I'D FIX FIRST**

* Run Solver. Data → Solver: objective `TOTAL_PROFIT`, Max; changing cells `Optimization!B6:B8`; constraints the seven your checks at `B35:B41` already state. Run it from both starting points in `Checks!A17:A18`, 0 / 0 / 0 and 20 / 0 / 0; expect 10 / 20 / 30 from both. Ten minutes.
* Leave the solved state in `B6:B8` and save in Excel, not from a script, so the Solver model and the calculated values are both in the file you commit.
* Fill the two-start table at `Checks!A17:D18` and the shadow prices at `Results!B18:B22` from Solver's sensitivity report.
* Write the audit of the solved build — what you checked, found, and did about it. Your Review reasons about a build that did not exist yet; the audit runs your rules against the file that came back.
* Three spec edits: `CAR_HRS` as 2.5/3; a tolerance on each acceptance figure; a check that `EFFECTIVE_RATE` reconciles to `TOTAL_LABOR_COST`.

**LOOKING AHEAD**

Solver first, because every blank in the file is waiting on it; then the audit; then the spec edits. Stage 1.3 reads its numbers from this workbook, so a solved, saved file unblocks the rest of the case.

---
